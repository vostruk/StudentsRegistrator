How to set-up backend server (Linux):
1. Create virtual environment `virtualenv --python=python3 venv`
2. Activate it `source venv/bin/activate`
3. Install required packages `pip install -r requirements.txt`
4. `cd apsi_backend`
5. Run tests `./manage.py test`
6. Run migrations `./manage.py migrate`
6. Run server `./manage.py runserver`

How to set-up frontend:
1. Make sure grunt is installed and npm is updated
2. Install dependencies (in apsi_frontend folder) 'npm install --save-dev'
3. Install dependencies by bower 'bower install'
4. Build grunt 'grunt'
5. Run grunt 'grunt serve' (use --force if something goes wrong)
6. localhost:9000 will be opened