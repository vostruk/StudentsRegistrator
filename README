How to set-up backend server (Linux):
1. Create virtual environment `virtualenv --python=python3 venv`
2. Activate it `source venv/bin/activate`
3. Install required packages `pip install -r requirements.txt`
4. `cd apsi_backend`
5. Run tests `./manage.py test`
6. Run migrations `./manage.py migrate`
6. Run server `./manage.py runserver`
